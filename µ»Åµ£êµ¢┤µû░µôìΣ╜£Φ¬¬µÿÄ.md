# KD Coffee 每月網站更新方式

## 最方便的方式（Chrome / Edge）

1. 打開 `tools/KD_Coffee_Website_Manager_v1.0.html`。
2. 按右上角「開啟網站資料」。
3. 選擇專案內的 `public/data/website-data.json`。
4. 修改本月活動、月份、作品上架、排序、售價與圖片路徑。
5. 按「檢查資料」。
6. 按「直接儲存」。
7. 回到正在執行的網站，重新整理瀏覽器。

網站資料已與版面程式分離。日後不需要修改 `data/monthlyMenu.ts` 或 `data/monthlyCampaign.ts`。

## 圖片放置方式

- 活動圖片：放入 `public/images/campaign/`
- 作品縮圖：放入 `public/images/works/`
- 作品廣告單：放入 `public/images/posters/`

管理工具內填寫路徑時，格式如下：

- `/images/campaign/2026-08-dripbag.webp`
- `/images/works/vangogh.webp`
- `/images/posters/vangogh-poster.webp`

檔名建議使用英文、數字與連字號，不要使用空格。

## 每月實際只需要做的事

- 更新活動名稱、說明、按鈕與圖片。
- 更新月份文字。
- 勾選本月上架作品。
- 勾選最多 4 件首頁精選。
- 用上下箭頭調整作品排序。
- 新品或推薦時填入角落標籤。
- 有價格異動時修改 NT$ 售價。

## 發布網站

本機確認後，將專案重新部署到正式主機。若日後接入正式線上後台，這份 `website-data.json` 結構可以直接延續，不必重做首頁與作品頁。


## 下載 JSON 後的下一步

如果管理工具顯示「已下載 website-data.json」，請將下載的檔案覆蓋到：

```text
專案資料夾/public/data/website-data.json
```

Windows 操作：

1. 在另存新檔視窗進入目前專案資料夾。
2. 依序進入 `public` → `data`。
3. 檔名保持 `website-data.json`。
4. 按「存檔」，遇到同名檔案時選擇覆蓋。
5. 回到網站按 `Ctrl + F5` 強制重新整理。

活動圖片不是存進 JSON。請先把圖片複製到：

```text
public/images/campaign/
```

管理工具的活動圖片欄位只填網站路徑，例如：

```text
/images/campaign/2026-mid-autumn.jpg
```
